plugins {
    id "com.android.application"
    // START: FlutterFire Configuration
    id 'com.google.gms.google-services'
    // END: FlutterFire Configuration
    id "kotlin-android"
    // The Flutter Gradle Plugin must be applied after the Android and Kotlin Gradle plugins.
    id "dev.flutter.flutter-gradle-plugin"
}

android {
    namespace 'com.example.virgilapp'  // <- add your app package here

    compileSdkVersion 35   // or compileSdk = 35 if using newer Gradle

    defaultConfig {
        applicationId "com.example.virgilapp"
        minSdkVersion 23    // make sure minSdkVersion >= 23 per your previous error
        targetSdkVersion 35
        versionCode 1
        versionName "1.0"
    }
android {
    // your existing config...

    compileOptions {
        sourceCompatibility JavaVersion.VERSION_1_8
        targetCompatibility JavaVersion.VERSION_1_8
    }

    kotlinOptions {
        jvmTarget = "1.8"
    }
}


    // other configurations ...
}

flutter {
    // flutter settings like sourceDirectory, etc.
}

flutter {
    source = "../.."
}
